# ── 依赖 ─────────────────────────────────────────────────────────────
node_modules/

# ── 构建产物 ─────────────────────────────────────────────────────────
# `src-tauri/target` 是这里最大的一项：debug + release 两份加起来 8.7 GB，
# 而且里面的 .d / .rustc_info.json 会把构建机的绝对路径原样写进去
# （`C:\Users\<你>\.cargo\registry\...`）。二进制文件本身没隐私，
# 但这些文本文件有，而且 GitHub 单文件 100 MB 也放不下。
/dist/
/src-tauri/target/

# Tauri CLI 每次 build 都重新生成的 ACL schema。
# 上游模板默认忽略它——它描述的是「当前构建的权限长什么样」，
# 不是权限本身（权限在 src-tauri/capabilities/ 里，那份要提交）。
/src-tauri/gen/schemas/

# ── 本地生成、含真实凭据，绝不能提交 ─────────────────────────────────
# 由 TypeStat 自己写出来（「适配器」页的「装进 WPS」），里面是真的端口和令牌。
# 它对每个使用者都是不同的（令牌是各自机器上随机生成的），
# 提交上去既泄露了提交者的令牌，也会让别人的加载项连不上。
# 需要的话照 wps-addon/js/config.example.js 抄一份。
/wps-addon/js/config.js

# ── 编辑器 / 系统 ────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*~
Thumbs.db
Desktop.ini
desktop.ini

# ── 杂项 ─────────────────────────────────────────────────────────────
*.log
*.tmp
